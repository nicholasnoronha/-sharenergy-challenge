import styled from "styled-components";

const Container = styled.div`
  display: flex;
  align-items: center;
  label {
    color: var(--dark-blue);
    font-size: 12px;
  }
`;

export default Container;
